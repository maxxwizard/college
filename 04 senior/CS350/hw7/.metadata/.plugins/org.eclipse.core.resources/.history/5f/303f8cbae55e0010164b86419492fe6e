/**
 * @author Matthew Huynh
 * 
 */
public class Main {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		final int N = 2;
		MyThread[] p = new MyThread[N];
		for (int i = 0; i < N; i++) {
			p[i] = new MyThread(i);
			p[i].start();
		}
	}
}
